fastapi==0.115.0
uvicorn[standard]==0.30.6
httpx==0.27.2

# pydantic 2.13+ é necessário para Python 3.13/3.14: as versões anteriores fixam
# pydantic-core <= 2.35, que não publica wheel para essas versões no Windows —
# o pip cairia na compilação em Rust e falharia sem um toolchain instalado.
pydantic>=2.13,<3
pydantic-settings>=2.7,<3

itsdangerous==2.2.0
cachetools==5.5.0
python-dotenv==1.0.1

# Fixados na major: ambos os SDKs mudam de major com frequência, e um `>=`
# aberto entregaria uma versão futura incompatível a quem clonar o projeto.
anthropic>=1.4,<2
openai>=3.8,<4
