# fastapi 0.141+: o SDK do MCP exige starlette >= 1.0, que a série 0.115 do
# fastapi ainda travava em < 0.39. Manter o par junto evita um conflito de
# resolução na instalação limpa.
fastapi>=0.141,<1
uvicorn[standard]==0.30.6
httpx==0.27.2
<<<<<<< HEAD

# pydantic 2.13+ é necessário para Python 3.13/3.14: as versões anteriores fixam
# pydantic-core <= 2.35, que não publica wheel para essas versões no Windows —
# o pip cairia na compilação em Rust e falharia sem um toolchain instalado.
pydantic>=2.13,<3
pydantic-settings>=2.7,<3

=======
pydantic>=2.9.2
pydantic-settings>=2.5.2
>>>>>>> 159c84aa20a29c88dffa974f84d744e63bdf7cf5
itsdangerous==2.2.0
cachetools==5.5.0
python-dotenv==1.0.1

# Fixados na major: ambos os SDKs mudam de major com frequência, e um `>=`
# aberto entregaria uma versão futura incompatível a quem clonar o projeto.
anthropic>=1.4,<2
openai>=3.8,<4

# Servidor MCP (app/mcp_server.py). Fixado na major: o protocolo ainda muda.
mcp>=1.2,<2

# Índice vetorial da busca semântica (app/vector_store.py). Embarcado:
# persiste em backend/.chroma e não pede serviço nenhum ao lado. Os
# embeddings rodam localmente em ONNX — sem chave de API e sem rate limit.
chromadb>=1.5,<2

# Agrupamento de faixas por clima (app/clustering.py): TF-IDF sobre as
# tags + k-means, com a silhueta escolhendo o k.
scikit-learn>=1.5,<2

# Testes (backend/tests) e conjunto dourado (backend/evals).
pytest>=8,<10
pytest-asyncio>=0.24,<2
pyyaml>=6,<7
